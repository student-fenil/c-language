#include <stdio.h>

int main()
{
    int i = 10;
    int j = i;
    int a = 20, b = 30, c = 40, d = 50;

    printf(" \n The value of i is %d and valuse of j is %d", i, j);
    printf(" \n The value of a is %d and valuse of b is %d", a, b);
    printf(" \n The value of c is %d and valuse of d is %d", c, d);

    return 0;
}

// %d is called format specifier
// %d is for int,%f is for float,%c is for char




/*output
 The value of i is 10 and valuse of j is 10 
 The value of a is 20 and valuse of b is 30 
 The value of c is 40 and valuse of d is 50
 */